// Generated by CoffeeScript 1.10.0
(function() {
  var Mood, Moods, _, config, mw;

  Mood = require('../models/moods');

  _ = require('underscore');

  config = require('../config');

  mw = require('../middleware');

  Moods = (function() {
    var create, get;

    function Moods(router, resource) {
      this.router = router;
      this.resource = resource;
      this.router.post("/", mw.validator.moods.post, create);
      this.router.get("/", get);
      config.app._405(this.router);
    }

    create = function(req, res) {
      var mood;
      mood = new Mood(req.body);
      return mood.save(function(e, mood) {
        if (e) {
          return res.status(500).json(e);
        } else {
          return res.json(mood);
        }
      });
    };

    get = function(req, res) {
      return console.log("%s: %s", req.method.toUpperCase(), req.url);
    };

    Moods.prototype.get_routes = function() {
      return this.router;
    };

    return Moods;

  })();

  module.exports = Moods;

}).call(this);
