// Generated by CoffeeScript 1.10.0
(function() {
  var Logout;

  Logout = (function() {
    var get, post;

    function Logout(router, resource) {
      this.router = router;
      this.resource = resource;
      this.router.post("/", post);
      this.router.get("/", get);
    }

    post = function(req, res) {
      return console.log("%s: %s", req.method.toUpperCase(), req.url);
    };

    get = function(req, res) {
      console.log("%s: %s", req.method.toUpperCase(), req.url);
      return res.json({
        success: true
      });
    };

    Logout.prototype.get_routes = function() {
      return this.router;
    };

    return Logout;

  })();

  module.exports = Logout;

}).call(this);
